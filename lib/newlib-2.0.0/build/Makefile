# GNU Make project makefile autogenerated by Premake

ifndef config
  config=debug
endif

ifndef verbose
  SILENT = @
endif

.PHONY: clean prebuild prelink

ifeq ($(config),debug)
  TARGETDIR = bin/Debug
  TARGET = $(TARGETDIR)/newlib
  define BUILDCMDS
	@echo Running build commands
	PATH=$$PATH:../../../lib/bin make -C ..
	cp ../x86_64-pc-packetngin/newlib/libc.a ../..
	cp ../x86_64-pc-packetngin/newlib/libm.a ../..
  endef
  define CLEANCMDS
	@echo Running clean commands
	make -C ../ clean
  endef
endif

ifeq ($(config),release)
  TARGETDIR = bin/Release
  TARGET = $(TARGETDIR)/newlib
  define BUILDCMDS
	@echo Running build commands
	PATH=$$PATH:../../../lib/bin make -C ..
	cp ../x86_64-pc-packetngin/newlib/libc.a ../..
	cp ../x86_64-pc-packetngin/newlib/libm.a ../..
  endef
  define CLEANCMDS
	@echo Running clean commands
	make -C ../ clean
  endef
endif

$(TARGET):
	$(BUILDCMDS)

clean:
	$(CLEANCMDS)
